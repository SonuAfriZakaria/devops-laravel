pipeline {
    agent any

    stages {
        stage('Install Dependencies') {
            steps {
                sh '''
                echo "Mulai install dependency..."
                '''
            }
        }

        stage('Test') {
            steps {
                sh '''
                echo "Testing berjalan..."
                '''
            }
        }

        stage('Deploy') {
            steps {
                sh '''
                echo "Deploy berjalan..."
                '''
            }
        }
    }
}
